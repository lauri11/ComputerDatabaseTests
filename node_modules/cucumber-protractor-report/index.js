var fs = require('fs'); 

function writeReport(filepath, contents, callback)
{
	fs.writeFile(filepath, contents, function(err)
	{
		if(err)
		{
			callback(err);
		}
		callback();
	});
}

module.exports = {

	writeReport: writeReport

};